-- CREATE TABLE salads (
--     id int NOT NULL AUTO_INCREMENT,
--     name VARCHAR(255) NOT NULL,
--     description VARCHAR(255) NOT NULL,
--     price DECIMAL NOT NULL,
--     PRIMARY KEY(id)
-- );

-- INSERT INTO salads (name, description, price) VALUES ("Cobbs McSalad", "This has kale in it. You've been warned", 6.99);

-- SELECT * FROM burgers;

-- ALTER TABLE salads MODIFY COLUMN price DECIMAL(10,2);

-- UPDATE salads SET 
-- price = 6.99, 
-- name = "Cobbies McSalad", 
-- description = "Kale is good for character" 
-- WHERE id = 1;

-- DELETE FROM burgers WHERE id = 3;
